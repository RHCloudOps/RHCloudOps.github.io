<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>AIX系统错误日志工具errpt详解</title>
    <url>/2025/03/11/AIX%E7%B3%BB%E7%BB%9F%E9%94%99%E8%AF%AF%E6%97%A5%E5%BF%97%E5%B7%A5%E5%85%B7errpt%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<h1 id="AIX系统错误日志工具errpt详解"><a href="#AIX系统错误日志工具errpt详解" class="headerlink" title="AIX系统错误日志工具errpt详解"></a>AIX系统错误日志工具errpt详解</h1><p><img src="https://rhcloudops.oss-cn-shenzhen.aliyuncs.com/img/IBM-AIX-errpt.jpg" alt="IBM-AIX-errpt"></p>
<h3 id="一、errpt-工具核心功能"><a href="#一、errpt-工具核心功能" class="headerlink" title="一、errpt 工具核心功能"></a>一、errpt 工具核心功能</h3><p><code>errpt</code>（Error Report Tool）是 AIX 系统中用于分析硬件、软件及操作系统错误的工具，功能包括：</p>
<ul>
<li><strong>错误分类</strong>：区分硬件（<code>H</code>）、软件（<code>S</code>）、操作系统（<code>O</code>）等错误类型。</li>
<li><strong>严重性分级</strong>：标记为 <code>Critical</code>（严重）、<code>Major</code>（主要）、<code>Minor</code>（次要）等。</li>
<li><strong>时间记录</strong>：精确记录错误发生时间，便于追溯。</li>
</ul>
<hr>
<h3 id="二、常用命令及参数解析"><a href="#二、常用命令及参数解析" class="headerlink" title="二、常用命令及参数解析"></a>二、常用命令及参数解析</h3><table>
<thead>
<tr>
<th>命令</th>
<th>说明</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td><code>errpt</code></td>
<td>列出所有错误日志（简略格式）</td>
<td><code>errpt</code></td>
</tr>
<tr>
<td><code>errpt -a</code></td>
<td>显示错误详细信息（包括修复建议）</td>
<td><code>errpt -a</code></td>
</tr>
<tr>
<td><code>errpt -d H</code></td>
<td>过滤显示硬件错误</td>
<td><code>errpt -d H</code></td>
</tr>
<tr>
<td><code>errpt -d S</code></td>
<td>过滤显示软件错误</td>
<td><code>errpt -d S</code></td>
</tr>
<tr>
<td><code>errpt -j &lt;ID&gt;</code></td>
<td>根据错误标识符查看详情</td>
<td><code>errpt -j BFE4C025</code></td>
</tr>
<tr>
<td><code>errpt -T PERM</code></td>
<td>显示永久性错误（需人工干预）</td>
<td><code>errpt -T PERM</code></td>
</tr>
<tr>
<td><code>errpt -s &lt;时间&gt;</code></td>
<td>按时间过滤错误（格式：<code>MMDDHHMMYY</code>）</td>
<td><code>errpt -s 0301000025</code>（2025年3月1日）</td>
</tr>
</tbody></table>
<hr>
<h3 id="三、故障案例分析流程"><a href="#三、故障案例分析流程" class="headerlink" title="三、故障案例分析流程"></a>三、故障案例分析流程</h3><h4 id="案例背景"><a href="#案例背景" class="headerlink" title="案例背景"></a>案例背景</h4><p>错误日志中频繁出现 <code>sysplanar0</code>（系统主板）相关错误，标识符为 <code>BFE4C025</code>，类型为 <code>PERM</code>（永久性错误）。</p>
<hr>
<h4 id="诊断步骤"><a href="#诊断步骤" class="headerlink" title="诊断步骤"></a>诊断步骤</h4><ol>
<li><p><strong>初步查看错误列表</strong>  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">errpt | <span class="built_in">head</span> -10   <span class="comment"># 列出最近10条错误日志</span></span><br></pre></td></tr></table></figure>
<p><strong>输出关键字段</strong>：</p>
<ul>
<li><code>IDENTIFIER</code>：错误唯一标识（如 <code>BFE4C025</code>）。</li>
<li><code>T</code>：错误类型（<code>P</code> 表示永久性错误，<code>T</code> 表示临时错误）。</li>
<li><code>DESCRIPTION</code>：错误描述（如 <code>UNDETERMINED ERROR</code>）。</li>
</ul>
</li>
<li><p><strong>查看错误详情</strong>  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">errpt -aj BFE4C025   <span class="comment"># 显示标识符为 BFE4C025 的详细日志</span></span><br></pre></td></tr></table></figure>
<p><strong>关键信息提取</strong>：</p>
<ul>
<li><strong>SRC（Service Request Code）</strong>：<code>B175F138</code><br>表示主板子系统预测性错误，需通过 IBM 文档或技术支持解码具体含义。</li>
<li><strong>维护建议</strong>：<code>Maintenance Procedure: FSPSP83</code><br>指向 IBM 提供的维护流程，通常涉及主板组件更换或固件升级。</li>
<li><strong>附加字段（Additional Words）</strong>：十六进制状态信息（如 <code>2BBD1F10</code>），需用 IBM 工具（如 <code>snap</code>）解析。</li>
</ul>
</li>
</ol>
<hr>
<h3 id="四、错误定位与处理建议"><a href="#四、错误定位与处理建议" class="headerlink" title="四、错误定位与处理建议"></a>四、错误定位与处理建议</h3><h4 id="1-核心错误位置"><a href="#1-核心错误位置" class="headerlink" title="1. 核心错误位置"></a>1. 核心错误位置</h4><ul>
<li><strong>资源名称</strong>：<code>sysplanar0</code>（系统主板）  </li>
<li><strong>资源类型</strong>：<code>sysplanar_rspc</code>（符合 CHRP 规范的主板）<br>可能涉及电源、总线或芯片组故障。</li>
</ul>
<h4 id="2-错误类型与操作"><a href="#2-错误类型与操作" class="headerlink" title="2. 错误类型与操作"></a>2. 错误类型与操作</h4><ul>
<li><strong>类型</strong>：<code>PERM</code>（永久性错误）<br>需立即处理，无法自愈，可能导致系统功能降级。</li>
<li><strong>建议操作</strong>：  <ul>
<li><strong>运行系统诊断</strong>：<code>diag</code> 命令启动硬件诊断工具。  </li>
<li><strong>联系 IBM 支持</strong>：根据 <code>SRC</code> 和 <code>Maintenance Procedure</code> 代码获取维护指导。  </li>
<li><strong>检查硬件状态</strong>：  <ul>
<li>电源模块电压稳定性。  </li>
<li>主板芯片组连接与固件版本。</li>
</ul>
</li>
</ul>
</li>
</ul>
<hr>
<h3 id="五、关键术语解释"><a href="#五、关键术语解释" class="headerlink" title="五、关键术语解释"></a>五、关键术语解释</h3><table>
<thead>
<tr>
<th>术语</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>SRC</strong></td>
<td>Service Request Code，IBM 硬件错误标识符，需官方文档解码。</td>
</tr>
<tr>
<td><strong>FRU</strong></td>
<td>Field Replaceable Unit，可更换硬件单元（如电源、内存条）。</td>
</tr>
<tr>
<td><strong>CHRP</strong></td>
<td>Common Hardware Reference Platform，IBM 硬件标准规范。</td>
</tr>
</tbody></table>
<hr>
]]></content>
      <categories>
        <category>IBM</category>
        <category>AIX</category>
      </categories>
      <tags>
        <tag>IBM</tag>
        <tag>AIX</tag>
        <tag>errpt</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux主机网卡（Bond）状态异常处理</title>
    <url>/2025/03/12/Linux%E4%B8%BB%E6%9C%BA%E7%BD%91%E5%8D%A1%EF%BC%88Bond%EF%BC%89%E7%8A%B6%E6%80%81%E5%BC%82%E5%B8%B8%E5%A4%84%E7%90%86/</url>
    <content><![CDATA[<h1 id="Linux主机网卡（Bond）状态异常处理"><a href="#Linux主机网卡（Bond）状态异常处理" class="headerlink" title="Linux主机网卡（Bond）状态异常处理"></a>Linux主机网卡（Bond）状态异常处理</h1><p><img src="https://rhcloudops.oss-cn-shenzhen.aliyuncs.com/img/Hardware-NetworkCard-bond.jpg" alt="Hardware-NetworkCard-bond"></p>
<h3 id="一、问题定位与关键命令解析"><a href="#一、问题定位与关键命令解析" class="headerlink" title="一、问题定位与关键命令解析"></a>一、问题定位与关键命令解析</h3><p>当Linux主机的Bond网卡出现非<code>up</code>状态时，可通过以下命令快速定位问题：</p>
<h4 id="1-统计非up状态的从属接口数量"><a href="#1-统计非up状态的从属接口数量" class="headerlink" title="1. 统计非up状态的从属接口数量"></a>1. 统计非<code>up</code>状态的从属接口数量</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cat</span> /proc/net/bonding/bond* | grep -A 1 <span class="string">&#x27;Slave Interface&#x27;</span> | awk <span class="string">&#x27;/Status/&#123;print $NF&#125;&#x27;</span> | grep -vc up</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>作用</strong>：统计所有Bond接口中状态非<code>up</code>的从属网卡数量。</li>
<li><strong>输出示例</strong>：<code>1</code>（表示有1个从属网卡异常）。</li>
<li><strong>说明</strong>：  <ul>
<li><code>cat /proc/net/bonding/bond*</code>：读取所有Bond接口的配置信息。  </li>
<li><code>grep -A 1 &#39;Slave Interface&#39;</code>：匹配包含<code>Slave Interface</code>的行，并输出该行及下一行（<code>-A 1</code>）。  </li>
<li><code>awk &#39;/Status/&#123;print $NF&#125;&#39;</code>：提取状态字段（<code>Status</code>行的最后一个值，即<code>up</code>或<code>down</code>）。  </li>
<li><code>grep -vc up</code>：统计非<code>up</code>状态的数量（<code>-v</code>反向匹配，<code>-c</code>计数）。</li>
</ul>
</li>
</ul>
<hr>
<h4 id="2-定位具体异常的从属网卡"><a href="#2-定位具体异常的从属网卡" class="headerlink" title="2. 定位具体异常的从属网卡"></a>2. 定位具体异常的从属网卡</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cat</span> /proc/net/bonding/bond* | grep -A 1 <span class="string">&#x27;Slave Interface&#x27;</span></span><br></pre></td></tr></table></figure>
<ul>
<li><p><strong>输出示例</strong>：  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Slave Interface: eth3</span><br><span class="line">MII Status: down</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>关键字段</strong>：  </p>
<ul>
<li><code>Slave Interface</code>：异常网卡名称（如 <code>eth3</code>）。  </li>
<li><code>MII Status</code>：物理链路状态（<code>down</code> 表示断开）。  </li>
<li><code>Link Failure Count</code>：链路故障次数（数值增加表明反复断开）。</li>
</ul>
</li>
</ul>
<hr>
<h4 id="3-查看详细Bond接口配置"><a href="#3-查看详细Bond接口配置" class="headerlink" title="3. 查看详细Bond接口配置"></a>3. 查看详细Bond接口配置</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cat</span> /proc/net/bonding/bond*</span><br></pre></td></tr></table></figure>
<ul>
<li><p><strong>输出关键字段解析</strong>：  </p>
<table>
<thead>
<tr>
<th>字段</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Bonding Mode</strong></td>
<td>Bond模式（如<code>802.3ad</code>动态链路聚合、<code>active-backup</code>主备模式）。</td>
</tr>
<tr>
<td><strong>MII Status</strong></td>
<td>Bond接口整体状态（<code>up</code>表示可用）。</td>
</tr>
<tr>
<td><strong>Slave Interface</strong></td>
<td>从属网卡名称及状态（重点关注<code>MII Status</code>）。</td>
</tr>
<tr>
<td><strong>Link Failure Count</strong></td>
<td>链路故障次数（非零值表示发生过故障）。</td>
</tr>
</tbody></table>
</li>
</ul>
<hr>
<h3 id="二、收集故障网卡信息"><a href="#二、收集故障网卡信息" class="headerlink" title="二、收集故障网卡信息"></a>二、收集故障网卡信息</h3><h4 id="1-获取网卡MAC地址"><a href="#1-获取网卡MAC地址" class="headerlink" title="1. 获取网卡MAC地址"></a>1. 获取网卡MAC地址</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ip a | grep eth3</span><br></pre></td></tr></table></figure>

<ul>
<li><p><strong>输出示例</strong>：  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">4: eth3: &lt;NO-CARRIER,BROADCAST,MULTICAST,SLAVE,UP&gt; ... link/ether 48:dc:2d:01:66:a5 ...</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>关键字段</strong>：<code>link/ether</code> 后为MAC地址（如 <code>48:dc:2d:01:66:a5</code>）。</p>
</li>
</ul>
<h4 id="2-获取Bond组IP地址"><a href="#2-获取Bond组IP地址" class="headerlink" title="2. 获取Bond组IP地址"></a>2. 获取Bond组IP地址</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ip a | grep bond1</span><br></pre></td></tr></table></figure>

<ul>
<li><p><strong>输出示例</strong>：  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">9: bond1: ... inet 192.168.142.43/24 ...</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>关键字段</strong>：<code>inet</code> 后为Bond组的IP地址（如 <code>192.168.142.43/24</code>）。</p>
</li>
</ul>
<h4 id="3-资产信息核对"><a href="#3-资产信息核对" class="headerlink" title="3. 资产信息核对"></a>3. 资产信息核对</h4><ul>
<li>根据主机IP或主机名，在资产表中确认设备地址和位置信息。</li>
</ul>
<hr>
<h3 id="三、信息提交网络组的模板"><a href="#三、信息提交网络组的模板" class="headerlink" title="三、信息提交网络组的模板"></a>三、信息提交网络组的模板</h3><p><strong>需包含以下信息</strong>：  </p>
<ol>
<li><strong>异常网卡</strong>：<code>eth3</code>  </li>
<li><strong>MAC地址</strong>：<code>48:dc:2d:01:66:a5</code>  </li>
<li><strong>所属Bond组</strong>：<code>bond1</code>  </li>
<li><strong>Bond组IP</strong>：<code>192.168.142.43/24</code>  </li>
<li><strong>物理位置</strong>：根据资产表提供设备地址和位置信息。</li>
</ol>
<hr>
<h3 id="四、故障原因分析与处理建议"><a href="#四、故障原因分析与处理建议" class="headerlink" title="四、故障原因分析与处理建议"></a>四、故障原因分析与处理建议</h3><h4 id="可能原因"><a href="#可能原因" class="headerlink" title="可能原因"></a>可能原因</h4><ol>
<li><strong>物理层问题</strong>：  <ul>
<li>网线松动、光模块故障、交换机端口异常。  </li>
<li><strong>操作建议</strong>：检查网线连接，更换光模块，验证交换机端口状态。</li>
</ul>
</li>
<li><strong>驱动或配置问题</strong>：  <ul>
<li>网卡驱动异常、Bond模式与交换机配置不匹配（如LACP未启用）。  </li>
<li><strong>操作建议</strong>：更新网卡驱动，确认交换机聚合组配置。</li>
</ul>
</li>
<li><strong>硬件故障</strong>：  <ul>
<li>网卡或主板接口损坏。  </li>
<li><strong>操作建议</strong>：更换网卡或主板。</li>
</ul>
</li>
</ol>
<h4 id="应急处理"><a href="#应急处理" class="headerlink" title="应急处理"></a>应急处理</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ifdown eth3 &amp;&amp; ifup eth3   <span class="comment"># 重启异常网卡（临时恢复）</span></span><br></pre></td></tr></table></figure>

<hr>
<h3 id="五、Bond模式说明"><a href="#五、Bond模式说明" class="headerlink" title="五、Bond模式说明"></a>五、Bond模式说明</h3><table>
<thead>
<tr>
<th>模式</th>
<th>特点</th>
<th>适用场景</th>
</tr>
</thead>
<tbody><tr>
<td><strong>802.3ad（动态链路聚合）</strong></td>
<td>多网卡负载均衡，需交换机支持LACP。</td>
<td>高带宽需求、冗余要求高的场景。</td>
</tr>
<tr>
<td><strong>active-backup（主备模式）</strong></td>
<td>仅主接口工作，备用接口在主接口故障时切换。</td>
<td>高可用性要求，对带宽需求不高。</td>
</tr>
</tbody></table>
<hr>
<h3 id="六、总结"><a href="#六、总结" class="headerlink" title="六、总结"></a>六、总结</h3><ul>
<li><strong>核心流程</strong>：定位异常网卡 → 收集MAC&#x2F;IP → 提交网络组 → 物理&#x2F;配置排查。  </li>
<li><strong>关键命令</strong>：<code>grep -A 1</code> 过滤上下文，<code>ip a</code> 查看网络配置。  </li>
<li><strong>预防措施</strong>：定期检查Bond状态，确保驱动和固件为最新版本。</li>
</ul>
]]></content>
      <categories>
        <category>硬件运维</category>
        <category>网卡</category>
      </categories>
      <tags>
        <tag>硬件运维</tag>
        <tag>网卡</tag>
      </tags>
  </entry>
  <entry>
    <title>使用storcli64工具定位与排查RAID卡告警</title>
    <url>/2025/03/10/%E4%BD%BF%E7%94%A8storcli64%E5%B7%A5%E5%85%B7%E5%AE%9A%E4%BD%8D%E4%B8%8E%E6%8E%92%E6%9F%A5RAID%E5%8D%A1%E5%91%8A%E8%AD%A6/</url>
    <content><![CDATA[<h1 id="使用storcli64工具定位与排查RAID卡告警"><a href="#使用storcli64工具定位与排查RAID卡告警" class="headerlink" title="使用storcli64工具定位与排查RAID卡告警"></a>使用storcli64工具定位与排查RAID卡告警</h1><p><img src="https://rhcloudops.oss-cn-shenzhen.aliyuncs.com/img/Hardware-RAID-storcli64.jpg" alt="Hardware-RAID-storcli64"></p>
<h3 id="一、核心操作流程"><a href="#一、核心操作流程" class="headerlink" title="一、核心操作流程"></a>一、核心操作流程</h3><h4 id="1-上传RAID卡管理工具"><a href="#1-上传RAID卡管理工具" class="headerlink" title="1.上传RAID卡管理工具"></a>1.上传RAID卡管理工具</h4><p>在Ansible管理机中找到RAID卡工具 <code>storcli64</code>，上传至目标主机：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /home/ansible</span><br><span class="line">scp ./storcli64 ***.***.***.***:/home/ansible  <span class="comment"># 将工具复制到目标主机的/home/ansible目录</span></span><br></pre></td></tr></table></figure>
<ul>
<li><strong><code>scp</code> 命令</strong>：用于跨主机安全传输文件。  </li>
<li><strong>目标路径</strong>：确保目标主机有写入权限（如 <code>/home/ansible</code>）。</li>
</ul>
<hr>
<h4 id="2-检查RAID卡状态"><a href="#2-检查RAID卡状态" class="headerlink" title="2.检查RAID卡状态"></a>2.检查RAID卡状态</h4><p>使用 <code>storcli64</code> 查看RAID卡（控制器 <code>/c0</code>）的详细信息：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/home/ansible/storcli64 /c0 show all  <span class="comment"># 显示RAID卡所有信息（物理盘、逻辑卷、状态等）</span></span><br></pre></td></tr></table></figure>
<ul>
<li><p><strong>关键输出字段解析</strong>：  </p>
<table>
<thead>
<tr>
<th>字段</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>EID:Slt</strong></td>
<td>物理盘位置（Enclosure ID:Slot）。</td>
</tr>
<tr>
<td><strong>DID</strong></td>
<td>磁盘唯一标识符。</td>
</tr>
<tr>
<td><strong>State</strong></td>
<td>磁盘状态：<code>Onln</code>（正常在线）、<code>Offln</code>（离线异常）。</td>
</tr>
<tr>
<td><strong>DG</strong></td>
<td>磁盘所属磁盘组（RAID组）。</td>
</tr>
<tr>
<td><strong>Model</strong></td>
<td>磁盘型号（如 <code>HGST HUS726T6TALE6L4</code>）。</td>
</tr>
</tbody></table>
</li>
<li><p><strong>故障判断</strong>：  </p>
<ul>
<li><p>若 <code>State</code> 显示 <code>Offln</code>（如 <code>0:1      11 Offln  2 ...</code>），表示该磁盘已离线，需进一步处理。 </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">0:1      11 Offln  2   5.456 TB SATA HDD N   N  512B HUS726060ALE610      U  - </span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>操作建议</strong>：  </p>
<ol>
<li>通过服务器管理口（如iLO&#x2F;iDRAC）确认物理硬盘故障指示灯状态。  </li>
<li>记录故障磁盘的 <code>EID:Slt</code>（如 <code>0:1</code>），准备更换。</li>
</ol>
</li>
</ul>
</li>
</ul>
<hr>
<h4 id="3-确认RAID卡型号"><a href="#3-确认RAID卡型号" class="headerlink" title="3.确认RAID卡型号"></a>3.确认RAID卡型号</h4><p>通过 <code>lspci</code> 查看RAID卡硬件型号：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lspci | grep -i lsi  <span class="comment"># 过滤显示LSI品牌的RAID控制器</span></span><br><span class="line">3b:00.0 RAID bus controller: LSI Logic / Symbios Logic MegaRAID SAS-3 3108 [Invader] (rev 02)</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>关键信息</strong>：  <ul>
<li><strong>型号</strong>：<code>MegaRAID SAS-3 3108 [Invader]</code>  </li>
<li><strong>兼容性</strong>：确认工具 <code>storcli64</code> 支持该型号（LSI RAID卡通用）。</li>
</ul>
</li>
</ul>
<hr>
<h3 id="二、关键命令解析"><a href="#二、关键命令解析" class="headerlink" title="二、关键命令解析"></a>二、关键命令解析</h3><table>
<thead>
<tr>
<th>命令</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>scp &lt;本地文件&gt; &lt;用户@IP:路径&gt;</code></td>
<td>跨主机安全复制文件（需SSH权限）。</td>
</tr>
<tr>
<td><code>storcli64 /c0 show all</code></td>
<td>显示RAID控制器 <code>/c0</code> 的详细信息，包括物理盘、逻辑卷状态。</td>
</tr>
<tr>
<td><code>lspci | grep -i lsi</code></td>
<td>列出所有PCI设备，过滤显示LSI品牌硬件（如RAID卡）。</td>
</tr>
</tbody></table>
<hr>
<h3 id="三、故障处理建议"><a href="#三、故障处理建议" class="headerlink" title="三、故障处理建议"></a>三、故障处理建议</h3><h4 id="1-离线磁盘处理"><a href="#1-离线磁盘处理" class="headerlink" title="1.离线磁盘处理"></a>1.离线磁盘处理</h4><ul>
<li><strong>物理更换</strong>：<br>根据 <code>EID:Slt</code> 定位故障硬盘（如 <code>0:1</code> 表示 Enclosure 0, Slot 1），断电后更换新硬盘。  </li>
<li><strong>重新加入阵列</strong>：  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/home/ansible/storcli64 /c0/e0/s1 <span class="built_in">set</span> online  <span class="comment"># 将Enclosure 0, Slot 1的磁盘重新上线</span></span><br></pre></td></tr></table></figure></li>
<li><strong>重建RAID</strong>（如需）：  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/home/ansible/storcli64 /c0/dg2 start rebuild  <span class="comment"># 对磁盘组2启动重建</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="2-日志清理（可选）"><a href="#2-日志清理（可选）" class="headerlink" title="2.日志清理（可选）"></a>2.日志清理（可选）</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/home/ansible/storcli64 /c0 delete logs  <span class="comment"># 清除RAID卡历史日志</span></span><br></pre></td></tr></table></figure>

<hr>
<h3 id="四、注意事项"><a href="#四、注意事项" class="headerlink" title="四、注意事项"></a>四、注意事项</h3><h4 id="1-权限问题："><a href="#1-权限问题：" class="headerlink" title="1.权限问题："></a>1.权限问题：</h4><ul>
<li><code>storcli64</code> 需以 <code>root</code> 权限运行，否则可能无法获取完整信息。  </li>
<li>若工具无执行权限，运行 <code>chmod +x /home/ansible/storcli64</code>。</li>
</ul>
<h4 id="2-数据备份："><a href="#2-数据备份：" class="headerlink" title="2.数据备份："></a>2.数据备份：</h4><ul>
<li>更换磁盘前，确认RAID冗余级别（如RAID 5&#x2F;6&#x2F;10），确保数据安全。</li>
</ul>
<h4 id="3-固件升级："><a href="#3-固件升级：" class="headerlink" title="3.固件升级："></a>3.固件升级：</h4><ul>
<li>若频繁出现磁盘离线，检查RAID卡固件版本，必要时升级：  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/home/ansible/storcli64 download firmware file=&lt;固件文件&gt;  <span class="comment"># 需从官网获取固件</span></span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<h3 id="五、总结"><a href="#五、总结" class="headerlink" title="五、总结"></a>五、总结</h3><ul>
<li><strong>核心步骤</strong>：上传工具 → 检查磁盘状态 → 定位故障 → 物理更换 → 重建阵列。  </li>
<li><strong>工具依赖</strong>：<code>storcli64</code> 是管理LSI RAID卡的核心工具，需与硬件型号匹配。  </li>
<li><strong>自动化扩展</strong>：可通过Ansible批量执行 <code>storcli64</code> 命令，实现多主机RAID状态监控。</li>
</ul>
]]></content>
      <categories>
        <category>硬件运维</category>
        <category>RAID</category>
      </categories>
      <tags>
        <tag>硬件运维</tag>
        <tag>RAID</tag>
        <tag>storcli64</tag>
      </tags>
  </entry>
  <entry>
    <title>sas3ircu &amp; storcli64工具定位故障硬盘位置</title>
    <url>/2025/03/15/sas3ircu%20&amp;%20storcli64%E5%B7%A5%E5%85%B7%E5%AE%9A%E4%BD%8D%E6%95%85%E9%9A%9C%E7%A1%AC%E7%9B%98%E4%BD%8D%E7%BD%AE/</url>
    <content><![CDATA[<h1 id="sas3ircu-storcli64工具定位故障硬盘位置"><a href="#sas3ircu-storcli64工具定位故障硬盘位置" class="headerlink" title="sas3ircu &amp; storcli64工具定位故障硬盘位置"></a>sas3ircu &amp; storcli64工具定位故障硬盘位置</h1><p><img src="https://rhcloudops.oss-cn-shenzhen.aliyuncs.com/img/Hardware-Disk-sas3ircu.jpg" alt="Hardware-Disk-sas3ircu"></p>
<h3 id="一、进入ansible目录找到服务器管理工具"><a href="#一、进入ansible目录找到服务器管理工具" class="headerlink" title="一、进入ansible目录找到服务器管理工具"></a>一、进入ansible目录找到服务器管理工具</h3><h4 id="命令-1：切换到-home-ansible-目录并列出文件"><a href="#命令-1：切换到-home-ansible-目录并列出文件" class="headerlink" title="命令 1：切换到 &#x2F;home&#x2F;ansible&#x2F; 目录并列出文件"></a>命令 1：切换到 &#x2F;home&#x2F;ansible&#x2F; 目录并列出文件</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /home/ansible/  </span><br><span class="line"><span class="built_in">ls</span>  </span><br></pre></td></tr></table></figure>
<ul>
<li><strong>作用</strong>：  <ul>
<li><code>cd /home/ansible/</code>：进入服务器管理工具目录。  </li>
<li><code>ls</code>：列出目录中的文件，确认工具（如 <code>storcli64</code> 和 <code>sas3ircu</code>）存在。</li>
</ul>
</li>
<li><strong>输出关键信息</strong>：<br>目录中包含多个管理脚本和工具，例如 <code>storcli64</code>（RAID管理工具）、<code>sas3ircu</code>（SAS控制器工具）等。</li>
</ul>
<hr>
<h4 id="命令-2：检查-RAID-控制器状态"><a href="#命令-2：检查-RAID-控制器状态" class="headerlink" title="命令 2：检查 RAID 控制器状态"></a>命令 2：检查 RAID 控制器状态</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/home/ansible/storcli64 /c0 show all  </span><br></pre></td></tr></table></figure>
<ul>
<li><strong>作用</strong>：使用 <code>storcli64</code> 工具查看控制器 0 的详细信息（包括磁盘组、物理磁盘状态等）。  </li>
<li><strong>输出关键信息</strong>：  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Status = Failure  </span><br><span class="line">Description = Controller 0 not found  </span><br></pre></td></tr></table></figure></li>
<li><strong>结论</strong>：  <ul>
<li><strong>RAID控制器 0 未被识别</strong>，可能原因包括：  <ul>
<li>控制器硬件故障或未正确连接。  </li>
<li>驱动未安装或工具版本不兼容。  </li>
<li>命令路径或参数错误。</li>
</ul>
</li>
</ul>
</li>
</ul>
<hr>
<h4 id="命令-3：检查-SAS-控制器与磁盘阵列状态"><a href="#命令-3：检查-SAS-控制器与磁盘阵列状态" class="headerlink" title="命令 3：检查 SAS 控制器与磁盘阵列状态"></a>命令 3：检查 SAS 控制器与磁盘阵列状态</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./sas3ircu 0 display  </span><br></pre></td></tr></table></figure>
<ul>
<li><strong>作用</strong>：使用 <code>sas3ircu</code> 工具查看 SAS 控制器 0 的配置信息，包括 RAID 卷状态和物理磁盘详情。  </li>
<li><strong>输出关键信息</strong>：  <ul>
<li><strong>IR Volume 1</strong>：  <ul>
<li>RAID 级别：RAID10  </li>
<li>状态：**Degraded (DGD)**（降级）  </li>
<li>关联物理硬盘位置：<code>Enclosure#/Slot#</code> 如 <code>1:2</code>, <code>0:0</code> 等。</li>
</ul>
</li>
<li><strong>IR Volume 2</strong>：  <ul>
<li>RAID 级别：RAID1  </li>
<li>状态：**Okay (OKY)**（正常）。</li>
</ul>
</li>
<li><strong>错误提示</strong>：  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SAS3IRCU: Error executing command DISPLAY.  </span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><strong>结论</strong>：  <ul>
<li>RAID10 卷处于降级状态，表明 <strong>至少一块硬盘失效</strong>，需定位具体故障盘。  </li>
<li>最后的错误提示可能因部分物理设备信息无法读取（如硬盘掉线或通信故障）。</li>
</ul>
</li>
</ul>
<hr>
<h3 id="二、故障定位与可能原因"><a href="#二、故障定位与可能原因" class="headerlink" title="二、故障定位与可能原因"></a>二、故障定位与可能原因</h3><h4 id="1-RAID-控制器问题："><a href="#1-RAID-控制器问题：" class="headerlink" title="1.RAID 控制器问题："></a>1.RAID 控制器问题：</h4><ul>
<li><code>storcli64</code> 无法识别控制器 0，可能为硬件故障或驱动问题。</li>
</ul>
<h4 id="2-RAID-卷降级："><a href="#2-RAID-卷降级：" class="headerlink" title="2.RAID 卷降级："></a>2.RAID 卷降级：</h4><ul>
<li>RAID10 卷降级说明部分硬盘失效，需根据 <code>sas3ircu</code> 输出的 <code>Enclosure#/Slot#</code>（如 <code>1:2</code>, <code>0:0</code>）定位具体槽位。</li>
</ul>
<h4 id="3-物理硬盘故障："><a href="#3-物理硬盘故障：" class="headerlink" title="3.物理硬盘故障："></a>3.物理硬盘故障：</h4><ul>
<li>降级卷关联的硬盘可能存在物理损坏、连接松动或电源问题。</li>
</ul>
<h4 id="4-工具兼容性或权限问题："><a href="#4-工具兼容性或权限问题：" class="headerlink" title="4.工具兼容性或权限问题："></a>4.工具兼容性或权限问题：</h4><ul>
<li><code>storcli64</code> 和 <code>sas3ircu</code> 版本与当前系统或硬件不兼容，或执行权限不足。</li>
</ul>
<hr>
<h3 id="三、-建议操作步骤"><a href="#三、-建议操作步骤" class="headerlink" title="三、 建议操作步骤"></a>三、 建议操作步骤</h3><h4 id="1-检查-RAID-控制器硬件："><a href="#1-检查-RAID-控制器硬件：" class="headerlink" title="1.检查 RAID 控制器硬件："></a>1.检查 RAID 控制器硬件：</h4><ul>
<li>确认控制器 0 的物理连接（如 PCIe插槽、电源线）。  </li>
<li>更新 RAID 控制器驱动或固件。</li>
</ul>
<h4 id="2-定位故障硬盘："><a href="#2-定位故障硬盘：" class="headerlink" title="2.定位故障硬盘："></a>2.定位故障硬盘：</h4><ul>
<li>根据 <code>sas3ircu</code> 输出的 <code>Enclosure#/Slot#</code>（如 <code>1:2</code>），找到对应槽位的硬盘，检查其状态灯（通常故障盘会亮红灯）。  </li>
<li>使用 <code>smartctl</code> 检查硬盘 SMART 健康状态：  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">smartctl -a /dev/sdX  </span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="3-修复-RAID-卷："><a href="#3-修复-RAID-卷：" class="headerlink" title="3.修复 RAID 卷："></a>3.修复 RAID 卷：</h4><ul>
<li>更换故障硬盘后，通过 <code>sas3ircu</code> 或 RAID 管理界面重建阵列。</li>
</ul>
<h4 id="4-排查工具问题："><a href="#4-排查工具问题：" class="headerlink" title="4.排查工具问题："></a>4.排查工具问题：</h4><ul>
<li>确认 <code>storcli64</code> 和 <code>sas3ircu</code> 版本与硬件兼容。  </li>
<li>以 <code>root</code> 权限执行命令，或检查文件执行权限：  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">chmod</span> +x /home/ansible/storcli64  </span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="5-查看系统日志："><a href="#5-查看系统日志：" class="headerlink" title="5.查看系统日志："></a>5.查看系统日志：</h4><ul>
<li>检查 <code>/var/log/messages</code> 或 <code>dmesg</code> 输出，获取更多硬件错误信息。</li>
</ul>
<hr>
<h3 id="四-总结"><a href="#四-总结" class="headerlink" title="四. 总结"></a>四. 总结</h3><ul>
<li><strong>当前问题</strong>：  <ul>
<li>RAID 控制器 0 无法识别，RAID10 卷降级，存在硬盘故障风险。  </li>
<li>SAS 控制器工具报错，可能因部分硬盘通信异常。</li>
</ul>
</li>
<li><strong>风险等级</strong>：<br><strong>高</strong>（降级卷可能导致数据丢失，需立即处理）。  </li>
<li><strong>处理优先级</strong>：  <ol>
<li>定位并更换故障硬盘，修复降级卷。  </li>
<li>排查 RAID 控制器硬件或驱动问题。  </li>
<li>验证工具兼容性，确保后续管理操作正常。</li>
</ol>
</li>
</ul>
]]></content>
      <categories>
        <category>硬件运维</category>
        <category>硬盘</category>
      </categories>
      <tags>
        <tag>硬件运维</tag>
        <tag>storcli64</tag>
        <tag>sas3ircu</tag>
        <tag>硬盘</tag>
      </tags>
  </entry>
  <entry>
    <title>通过ipmitool工具查找管理口ip</title>
    <url>/2025/03/14/%E9%80%9A%E8%BF%87ipmitool%E5%B7%A5%E5%85%B7%E6%9F%A5%E6%89%BE%E7%AE%A1%E7%90%86%E5%8F%A3ip/</url>
    <content><![CDATA[<h1 id="通过ipmitool工具查找管理口ip"><a href="#通过ipmitool工具查找管理口ip" class="headerlink" title="通过ipmitool工具查找管理口ip"></a>通过ipmitool工具查找管理口ip</h1><p><img src="https://rhcloudops.oss-cn-shenzhen.aliyuncs.com/img/Linux-ManagementTools-ipmitool.jpg" alt="Linux-ManagementTools-ipmitool"></p>
<h2 id="一、查看服务器管理口的网络配置信息"><a href="#一、查看服务器管理口的网络配置信息" class="headerlink" title="一、查看服务器管理口的网络配置信息"></a>一、查看服务器管理口的网络配置信息</h2><h3 id="1-命令：ipmitool-lan-print"><a href="#1-命令：ipmitool-lan-print" class="headerlink" title="1.命令：ipmitool lan print"></a>1.命令：ipmitool lan print</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ipmitool lan <span class="built_in">print</span></span><br></pre></td></tr></table></figure>
<ul>
<li><strong>作用</strong>：<br>查看服务器管理口（BMC&#x2F;iLO&#x2F;iDRAC）的网络配置信息，包括IP地址、子网掩码、网关等。  </li>
<li><strong>适用场景</strong>：<br>当服务器管理口IP丢失、配置错误或无法通过常规方式访问时，通过该命令可快速确认当前管理口的网络状态。</li>
</ul>
<hr>
<h3 id="2-命令输出关键信息解析"><a href="#2-命令输出关键信息解析" class="headerlink" title="2. 命令输出关键信息解析"></a>2. 命令输出关键信息解析</h3><p>执行 <code>ipmitool lan print</code> 后输出如下：  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Set in Progress         : Set Complete        # 配置已生效  </span><br><span class="line">IP Address Source       : Static Address     # IP地址来源为静态分配  </span><br><span class="line">IP Address              : 192.168.141.78     # 管理口当前IP地址  </span><br><span class="line">Subnet Mask             : 255.255.255.0      # 子网掩码  </span><br><span class="line">MAC Address             : 48:dc:2d:01:66:a0  # 管理口MAC地址  </span><br><span class="line">Default Gateway IP      : 192.168.141.254    # 默认网关  </span><br><span class="line">802.1q VLAN ID          : Disabled           # VLAN未启用  </span><br></pre></td></tr></table></figure>
<ul>
<li><strong>结论</strong>：  <ul>
<li>当前管理口已配置静态IP <code>192.168.141.78</code>，子网掩码和网关正常。  </li>
<li>若无法通过该IP访问管理口，可能原因包括：  <ul>
<li>网络物理连接故障（如网线未插或交换机端口问题）。  </li>
<li>IP地址冲突或被防火墙拦截。  </li>
<li>管理口硬件故障（如BMC模块异常）。</li>
</ul>
</li>
</ul>
</li>
</ul>
<hr>
<h2 id="二、管理口IP问题的排查步骤"><a href="#二、管理口IP问题的排查步骤" class="headerlink" title="二、管理口IP问题的排查步骤"></a>二、管理口IP问题的排查步骤</h2><h3 id="1-确认管理口网络配置："><a href="#1-确认管理口网络配置：" class="headerlink" title="1.确认管理口网络配置："></a>1.确认管理口网络配置：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ipmitool lan <span class="built_in">print</span>  <span class="comment"># 检查IP、子网掩码、网关是否配置正确</span></span><br></pre></td></tr></table></figure>

<ul>
<li>若IP为 <code>0.0.0.0</code>，说明未分配，需手动设置静态IP或启用DHCP。</li>
</ul>
<h3 id="2-测试网络连通性："><a href="#2-测试网络连通性：" class="headerlink" title="2.测试网络连通性："></a>2.测试网络连通性：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ping 192.168.141.78       <span class="comment"># 测试管理口IP是否可达  </span></span><br><span class="line">arp -n | grep 48:dc:2d    <span class="comment"># 根据MAC地址确认IP是否在局域网中可见  </span></span><br></pre></td></tr></table></figure>

<h3 id="3-检查硬件与驱动："><a href="#3-检查硬件与驱动：" class="headerlink" title="3.检查硬件与驱动："></a>3.检查硬件与驱动：</h3><ul>
<li>确认服务器BMC模块已启用（部分服务器需在BIOS中开启IPMI功能）。  </li>
<li>检查网线连接和管理口指示灯状态。</li>
</ul>
<h2 id="三、ipmitool常用命令及用法详解"><a href="#三、ipmitool常用命令及用法详解" class="headerlink" title="三、ipmitool常用命令及用法详解"></a>三、ipmitool常用命令及用法详解</h2><h3 id="1-基本连接命令"><a href="#1-基本连接命令" class="headerlink" title="1. 基本连接命令"></a><strong>1. 基本连接命令</strong></h3><ul>
<li><p><strong>本地 BMC 访问</strong>（需 root 权限）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ipmitool -I open &lt;<span class="built_in">command</span>&gt;  <span class="comment"># 通过本地接口直接访问</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>远程 BMC 访问</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool -I lanplus -H &lt;BMC_IP&gt; -U &lt;用户名&gt; -P &lt;密码&gt; &lt;command&gt;</span><br></pre></td></tr></table></figure>

<ul>
<li><code>-I lanplus</code>：使用加密的 LAN 协议（推荐）。</li>
<li><code>-H</code>：BMC 的 IP 地址。</li>
<li><code>-U</code>&#x2F;<code>-P</code>：BMC 的用户名和密码。</li>
</ul>
</li>
</ul>
<hr>
<h3 id="2-电源管理"><a href="#2-电源管理" class="headerlink" title="2. 电源管理"></a><strong>2. 电源管理</strong></h3><ul>
<li><p><strong>查看电源状态</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool power status</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>开机</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool power on</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>关机</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool power off</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>强制关机</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool power off -f  # 强制断电（类似拔电）</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>重启</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool power reset</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>软关机</strong>（优雅关机）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool power soft</span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<h3 id="3-传感器监控"><a href="#3-传感器监控" class="headerlink" title="3. 传感器监控"></a><strong>3. 传感器监控</strong></h3><ul>
<li><p><strong>查看所有传感器数据</strong>（温度、电压、风扇等）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool sensor list</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>筛选特定传感器</strong>（如 CPU 温度）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool sensor get &quot;CPU Temp&quot;</span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<h3 id="4-FRU（现场可更换单元）信息"><a href="#4-FRU（现场可更换单元）信息" class="headerlink" title="4. FRU（现场可更换单元）信息"></a><strong>4. FRU（现场可更换单元）信息</strong></h3><ul>
<li><p><strong>显示 FRU 信息</strong>（主板、产品序列号等）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool fru print</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>导出 FRU 数据到文件</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool fru read &lt;file.bin&gt;</span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<h3 id="5-BMC-配置"><a href="#5-BMC-配置" class="headerlink" title="5. BMC 配置"></a><strong>5. BMC 配置</strong></h3><ul>
<li><p><strong>重置 BMC</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool mc reset [cold|warm]</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>查看 BMC 信息</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool mc info</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>设置 BMC 网络</strong>（静态 IP）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool lan set 1 ipsrc static</span><br><span class="line">ipmitool lan set 1 ipaddr &lt;IP&gt;</span><br><span class="line">ipmitool lan set 1 netmask &lt;NETMASK&gt;</span><br><span class="line">ipmitool lan set 1 defgw &lt;GATEWAY&gt;</span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<h3 id="6-SEL（系统事件日志）"><a href="#6-SEL（系统事件日志）" class="headerlink" title="6. SEL（系统事件日志）"></a><strong>6. SEL（系统事件日志）</strong></h3><ul>
<li><p><strong>查看所有日志</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool sel list</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>清除日志</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool sel clear</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>获取特定日志详情</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool sel get &lt;ID&gt;</span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<h3 id="7-用户管理"><a href="#7-用户管理" class="headerlink" title="7. 用户管理"></a><strong>7. 用户管理</strong></h3><ul>
<li><p><strong>列出用户</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool user list 1  # 1 是通道号（通常为1）</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>添加用户</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool user set name &lt;用户ID&gt; &lt;用户名&gt;</span><br><span class="line">ipmitool user set password &lt;用户ID&gt; &lt;密码&gt;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>设置用户权限</strong>（如管理员）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool channel setaccess 1 &lt;用户ID&gt; callin=on ipmi=on link=on privilege=4</span><br></pre></td></tr></table></figure>

<ul>
<li><code>privilege=4</code> 表示管理员权限（1-4 分别对应不同权限等级）。</li>
</ul>
</li>
</ul>
<hr>
<h3 id="8-远程控制台（SOL）"><a href="#8-远程控制台（SOL）" class="headerlink" title="8. 远程控制台（SOL）"></a><strong>8. 远程控制台（SOL）</strong></h3><ul>
<li><p><strong>启动 Serial Over LAN（SOL）</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool sol activate</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>退出 SOL</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool sol deactivate</span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<h3 id="9-其他实用命令"><a href="#9-其他实用命令" class="headerlink" title="9. 其他实用命令"></a><strong>9. 其他实用命令</strong></h3><ul>
<li><p><strong>查看支持的 IPMI 命令</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool -h</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>执行原始 IPMI 命令</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ipmitool raw &lt;HEX_COMMAND&gt;</span><br></pre></td></tr></table></figure>

<p>（例如：<code>ipmitool raw 0x06 0x01</code> 查看设备 ID）</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Linux管理工具</category>
        <category>ipmitool</category>
      </categories>
      <tags>
        <tag>Linux管理工具</tag>
        <tag>ipmitool</tag>
        <tag>服务器管理口</tag>
      </tags>
  </entry>
  <entry>
    <title>日志messages中有IO和文件系统报错</title>
    <url>/2025/03/16/%E6%97%A5%E5%BF%97messages%E4%B8%AD%E6%9C%89IO%E5%92%8C%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%E6%8A%A5%E9%94%99/</url>
    <content><![CDATA[<h1 id="日志messages中有IO和文件系统报错"><a href="#日志messages中有IO和文件系统报错" class="headerlink" title="日志messages中有IO和文件系统报错"></a>日志messages中有IO和文件系统报错</h1><p><img src="https://rhcloudops.oss-cn-shenzhen.aliyuncs.com/img/Linux-messages-XFSIOEXT4.jpg" alt="Linux-messages-XFSIOEXT4"></p>
<h3 id="一、-检查日志中的I-O和文件系统报错"><a href="#一、-检查日志中的I-O和文件系统报错" class="headerlink" title="一、 检查日志中的I&#x2F;O和文件系统报错"></a>一、 检查日志中的I&#x2F;O和文件系统报错</h3><p><strong>命令含义</strong>：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cat</span> /var/log/messages | grep <span class="string">&#x27;kernel&#x27;</span> | egrep -i <span class="string">&#x27;error|fail|readonly|abort|offline|fatal&#x27;</span> | egrep -i <span class="string">&#x27;ext.-fs|I/O|xfs-fs&#x27;</span></span><br></pre></td></tr></table></figure>
<ul>
<li><p><strong>作用</strong>：从系统日志 <code>/var/log/messages</code> 中筛选出与内核（<code>kernel</code>）相关的错误信息，进一步过滤出包含 <strong>I&#x2F;O错误、文件系统异常（ext4、XFS）</strong> 的关键字条目（如 <code>error</code>, <code>fail</code>, <code>readonly</code> 等）。  </p>
</li>
<li><p><strong>输出关键信息</strong>：  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Mar 11 14:41:11 DSJ-FS-FI-1441 kernel: [334658.278904] blk_update_request: I/O error, dev sdd, sector 5855646432  </span><br><span class="line">Mar 11 14:41:11 DSJ-FS-FI-1441 kernel: [334658.279098] blk_update_request: I/O error, dev sdd, sector 5855512576  </span><br><span class="line">Mar 11 14:41:11 DSJ-FS-FI-1441 kernel: [334658.279120] Buffer I/O error on dev sdd1, logical block 731938816, lost sync page write  </span><br></pre></td></tr></table></figure></li>
<li><p><strong>结论</strong>：  </p>
<ul>
<li>设备 <code>sdd</code> 出现 <strong>I&#x2F;O错误</strong>，涉及特定扇区和逻辑块，可能是物理磁盘损坏、连接故障或文件系统损坏。  </li>
<li><strong>需立即通知二线备份团队（Backup）进行应急处理</strong>，并通知应用团队检查业务是否受影响。</li>
</ul>
</li>
</ul>
<hr>
<h3 id="二、-查看磁盘状态（是否掉盘）"><a href="#二、-查看磁盘状态（是否掉盘）" class="headerlink" title="二、. 查看磁盘状态（是否掉盘）"></a>二、. 查看磁盘状态（是否掉盘）</h3><p><strong>命令含义</strong>：  </p>
<ul>
<li><p>**<code>/home/ansible/storcli64 /c0 show all</code>**：<br>使用 RAID 管理工具 <code>storcli64</code> 检查控制器 <code>/c0</code> 下所有物理磁盘（PD）的状态。  </p>
<ul>
<li><strong>输出关键信息</strong>：<br>所有磁盘状态为 <code>Onln</code>（在线），DG（驱动器组）值为 1-10，无 <code>Offln</code>（离线）或 <code>UBad</code>（异常）磁盘。  </li>
<li><strong>结论</strong>：<br>磁盘阵列中 <strong>未检测到掉盘</strong>，硬件层面未发现全局故障。</li>
</ul>
</li>
<li><p>**<code>lsblk</code>**：<br>列出所有块设备及其挂载信息，确认文件系统挂载点正常。  </p>
<ul>
<li><strong>输出关键信息</strong>：<br><code>sdd</code> 对应的分区 <code>sdd1</code> 挂载至 <code>/srv/BigData/hadoop/data*</code>，未显示异常状态。  </li>
<li><strong>结论</strong>：<br>date也为1-10证明没有掉盘，逻辑层面挂载正常，但需结合日志进一步排查 <code>sdd</code> 的硬件健康状态。</li>
</ul>
</li>
</ul>
<hr>
<h3 id="三、综合分析"><a href="#三、综合分析" class="headerlink" title="三、综合分析"></a>三、综合分析</h3><h4 id="1-问题定位："><a href="#1-问题定位：" class="headerlink" title="1.问题定位："></a>1.问题定位：</h4><ul>
<li>日志显示 <code>sdd</code> 存在 <strong>I&#x2F;O错误</strong>，可能是物理磁盘损坏、数据线接触不良或文件系统故障。  </li>
<li>磁盘阵列状态正常（无掉盘），说明问题可能局限于单盘 <code>sdd</code>，而非全局存储故障。</li>
</ul>
<h4 id="2-建议操作："><a href="#2-建议操作：" class="headerlink" title="2.建议操作："></a>2.建议操作：</h4><ul>
<li><strong>立即行动</strong>：  <ul>
<li>通知二线备份团队介入，启动业务应急预案。  </li>
<li>检查 <code>/srv/BigData/hadoop/data*</code> 的业务是否受影响（如数据写入失败、服务中断）。</li>
</ul>
</li>
<li><strong>进一步排查</strong>：  <ul>
<li>使用 <code>smartctl</code> 检查 <code>sdd</code> 的 SMART 健康状态。  </li>
<li>检查磁盘连接线、控制器插槽是否松动。  </li>
<li>尝试对 <code>sdd</code> 执行文件系统修复（如 <code>xfs_repair</code> 或 <code>fsck</code>）。</li>
</ul>
</li>
<li><strong>长期建议</strong>：  <ul>
<li>监控磁盘 I&#x2F;O 错误率，提前替换潜在故障盘。  </li>
<li>优化日志监控告警规则，确保及时响应。</li>
</ul>
</li>
</ul>
<hr>
]]></content>
      <categories>
        <category>Linux文件系统</category>
        <category>messages</category>
      </categories>
      <tags>
        <tag>硬盘</tag>
        <tag>messages</tag>
        <tag>文件系统</tag>
      </tags>
  </entry>
  <entry>
    <title>var文件目录使用率超过百分之95</title>
    <url>/2025/03/13/%E4%B8%BB%E6%9C%BAvar%E6%96%87%E4%BB%B6%E7%9B%AE%E5%BD%95%E4%BD%BF%E7%94%A8%E7%8E%87%E8%B6%85%E8%BF%87%E7%99%BE%E5%88%86%E4%B9%8B95/</url>
    <content><![CDATA[<h1 id="主机var文件目录使用率超过百分之95"><a href="#主机var文件目录使用率超过百分之95" class="headerlink" title="主机var文件目录使用率超过百分之95"></a>主机var文件目录使用率超过百分之95</h1><p><img src="https://rhcloudops.oss-cn-shenzhen.aliyuncs.com/img/FileSystem-var-95percent.jpg" alt="FileSystem-var-95percent"></p>
<h2 id="一、问题背景"><a href="#一、问题背景" class="headerlink" title="一、问题背景"></a>一、问题背景</h2><p>主机 <code>/var</code> 目录使用率超过95%，需紧急清理以释放空间。  </p>
<ul>
<li><strong>关键命令与输出</strong>：  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">df</span> -h /var  </span><br><span class="line">Filesystem                 Size  Used Avail Use% Mounted on  </span><br><span class="line">/dev/mapper/rootvg-lv_var   20G   20G  983M  96% /var  </span><br></pre></td></tr></table></figure>
<ul>
<li><strong>结论</strong>：<code>/var</code> 目录所在逻辑卷已接近满载（96%），需立即清理。</li>
</ul>
</li>
</ul>
<hr>
<h2 id="二、定位大文件-目录"><a href="#二、定位大文件-目录" class="headerlink" title="二、定位大文件&#x2F;目录"></a>二、定位大文件&#x2F;目录</h2><p><strong>进入 <code>/var</code> 目录并分析占用</strong>：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /var  </span><br><span class="line"><span class="built_in">du</span> -sm * 2&gt;/dev/null | <span class="built_in">sort</span> -rn | <span class="built_in">head</span>  </span><br></pre></td></tr></table></figure>
<ul>
<li><strong>作用</strong>：  <ul>
<li><code>du -sm *</code>：统计当前目录下所有子目录&#x2F;文件的大小（以MB为单位）。  </li>
<li><code>2&gt;/dev/null</code>：忽略无权限访问的目录报错。  </li>
<li><code>sort -rn</code>：按占用空间降序排序。  </li>
<li><code>head</code>：显示前10大占用项。</li>
</ul>
</li>
<li><strong>输出关键信息</strong>：  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">9740    account  # 占用9.7GB  </span><br><span class="line">9110    adm      # 占用9.1GB  </span><br><span class="line">8858    log      # 占用8.8GB  </span><br></pre></td></tr></table></figure></li>
<li><strong>结论</strong>：<code>account</code> 和 <code>log</code> 是主要占用目录，需优先清理。</li>
</ul>
<hr>
<h2 id="三、清理-var-log-目录"><a href="#三、清理-var-log-目录" class="headerlink" title="三、清理 &#x2F;var&#x2F;log 目录"></a>三、清理 &#x2F;var&#x2F;log 目录</h2><h3 id="1-进入-var-log-并分析文件"><a href="#1-进入-var-log-并分析文件" class="headerlink" title="1.进入 &#x2F;var&#x2F;log 并分析文件"></a>1.进入 &#x2F;var&#x2F;log 并分析文件</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /var/log  </span><br><span class="line"><span class="built_in">du</span> -sm * 2&gt;/dev/null | <span class="built_in">sort</span> -rn | <span class="built_in">head</span>  </span><br></pre></td></tr></table></figure>
<ul>
<li><strong>输出关键信息</strong>：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">4134    secure             # 4.1GB  </span><br><span class="line">2145    Bigdata            # 2.1GB  </span><br><span class="line">1257    cron               # 1.2GB  </span><br><span class="line">9       messages-2025****  # 旧日志文件  </span><br></pre></td></tr></table></figure>

<h4 id="1-1-var-log-secure-文件"><a href="#1-1-var-log-secure-文件" class="headerlink" title="1.1 &#x2F;var&#x2F;log&#x2F;secure 文件"></a>1.1 &#x2F;var&#x2F;log&#x2F;secure 文件</h4><p><code>/var/log/secure</code> 是 Linux 系统中记录安全相关信息的日志文件，主要用于存储身份验证和授权相关的日志，如用户登录、<code>sudo</code> 使用、远程登录等。它帮助系统管理员监控系统的安全状态。</p>
<p><strong>主要内容</strong></p>
<ol>
<li><strong>用户登录</strong>：记录本地和远程登录的成功与失败。</li>
<li><strong><code>sudo</code> 使用</strong>：记录用户通过 <code>sudo</code> 执行的命令。</li>
<li><strong>远程登录</strong>：记录通过 SSH 等工具的远程登录情况。</li>
<li><strong>认证失败</strong>：记录失败的登录尝试，帮助识别潜在攻击。</li>
</ol>
<h4 id="1-2-var-log-messages-文件"><a href="#1-2-var-log-messages-文件" class="headerlink" title="1.2 &#x2F;var&#x2F;log&#x2F;messages 文件"></a>1.2 &#x2F;var&#x2F;log&#x2F;messages 文件</h4><p><code>/var/log/messages</code> 是 Linux 系统中一个重要的系统日志文件，用于记录内核、系统服务和其他应用程序的通用消息。它帮助系统管理员监控系统运行状态、排查问题。</p>
<p><strong>主要内容</strong></p>
<ol>
<li><strong>内核消息</strong>：硬件检测、设备驱动加载等。</li>
<li><strong>系统服务日志</strong>：启动、停止、错误等信息。</li>
<li><strong>应用程序日志</strong>：部分应用程序的日志。</li>
<li><strong>系统事件</strong>：如网络连接、文件系统挂载等。</li>
</ol>
<p><code>/var/log/messages-20250315</code> 文件是<code> /var/log/messages</code> 日志文件的一个归档版本。通常，这个文件包含了在 2025年3月15日（根据文件名中的日期部分”20250315”可以推断）这一天及其之前记录的系统消息和事件信息。Linux 系统通过一种称为日志轮转(log rotation)的机制来管理日志文件，以防止日志文件无限增长占用过多磁盘空间。</p>
<h3 id="2-置空secure文件："><a href="#2-置空secure文件：" class="headerlink" title="2.置空secure文件："></a>2.置空secure文件：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&gt; secure  <span class="comment"># 清空文件内容（保留文件句柄，避免服务中断）  </span></span><br></pre></td></tr></table></figure>

<h3 id="3-删除两个月前的messages日志："><a href="#3-删除两个月前的messages日志：" class="headerlink" title="3.删除两个月前的messages日志："></a>3.删除两个月前的messages日志：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">rm</span> -f messages-2024*    <span class="comment"># 删除2024年的旧日志  </span></span><br><span class="line"><span class="built_in">rm</span> -f messages-202501*  <span class="comment"># 删除2025年1月的旧日志  </span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="四、清理-var-account-目录"><a href="#四、清理-var-account-目录" class="headerlink" title="四、清理 &#x2F;var&#x2F;account 目录"></a>四、清理 &#x2F;var&#x2F;account 目录</h2><p>在 Linux 中，<code>/var/account</code> 目录用于存放与<strong>进程账户统计</strong>相关的数据，主要用于系统管理员跟踪和审计用户在系统上的活动。这个目录通常由 <code>acct</code>（进程账户）或者 <code>psacct</code>（进程统计）软件包管理。</p>
<h3 id="1-pacct文件的作用"><a href="#1-pacct文件的作用" class="headerlink" title="1.pacct文件的作用"></a>1.pacct文件的作用</h3><p><code>pacct</code> 文件（<code>/var/account/pacct</code>）是<strong>进程账户记录文件</strong>，它详细记录了系统上运行的每个进程的信息，包括：</p>
<ul>
<li>用户运行的命令</li>
<li>进程启动和结束的时间</li>
<li>进程消耗的 CPU 时间</li>
<li>进程的 UID（用户 ID）和 GID（组 ID）</li>
<li>进程的退出状态</li>
</ul>
<p>该文件主要由 <code>acct</code> 或 <code>psacct</code> 守护进程自动生成并更新。</p>
<h3 id="2-进入-var-account-并分析文件"><a href="#2-进入-var-account-并分析文件" class="headerlink" title="2.进入 &#x2F;var&#x2F;account 并分析文件"></a>2.进入 &#x2F;var&#x2F;account 并分析文件</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /var/account  </span><br><span class="line"><span class="built_in">du</span> -sm * 2&gt;/dev/null | <span class="built_in">sort</span> -rn | <span class="built_in">head</span>  </span><br></pre></td></tr></table></figure>
<ul>
<li><strong>输出关键信息</strong>：  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1490    pacct             # 1.4GB（当前进程记账文件）  </span><br><span class="line">107     pacct-2025****   # 压缩后的旧记账文件（每个约100MB）  </span><br></pre></td></tr></table></figure></li>
<li><strong>清理操作</strong>：  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">rm</span> -f pacct-2024*    <span class="comment"># 删除2024年的旧文件  </span></span><br><span class="line"><span class="built_in">rm</span> -f pacct-202501*  <span class="comment"># 删除2025年1月的旧文件  </span></span><br><span class="line"><span class="built_in">rm</span> -f pacct-202502*  <span class="comment"># 删除2025年2月的旧文件  </span></span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<h2 id="五、清理结果验证"><a href="#五、清理结果验证" class="headerlink" title="五、清理结果验证"></a>五、清理结果验证</h2><p><strong>验证命令</strong>：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">df</span> -h /var  </span><br><span class="line">Filesystem                 Size  Used Avail Use% Mounted on  </span><br><span class="line">/dev/mapper/rootvg-lv_var   20G   14G  7.0G  66% /var  </span><br></pre></td></tr></table></figure>
<ul>
<li><strong>结论</strong>：清理后 <code>/var</code> 使用率从 <strong>96%</strong> 降至 **66%**，空间释放成功。</li>
</ul>
<hr>
<h2 id="六、关键命令解释"><a href="#六、关键命令解释" class="headerlink" title="六、关键命令解释"></a>六、关键命令解释</h2><table>
<thead>
<tr>
<th>命令&#x2F;操作</th>
<th>作用说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>df -h /var</code></strong></td>
<td>查看 <code>/var</code> 目录所在文件系统的磁盘使用情况（<code>-h</code> 表示人类可读格式）。</td>
</tr>
<tr>
<td><strong><code>du -sm *</code></strong></td>
<td>统计当前目录下所有子目录&#x2F;文件的大小（<code>-s</code> 汇总总大小，<code>-m</code> 以MB为单位）。</td>
</tr>
<tr>
<td><strong><code>&gt; secure</code></strong></td>
<td>清空 <code>secure</code> 文件内容（保留文件句柄，避免服务重启）。</td>
</tr>
<tr>
<td><strong><code>rm -f messages-2024*</code></strong></td>
<td>强制删除匹配文件名模式的旧日志文件（<code>-f</code> 忽略不存在的文件）。</td>
</tr>
<tr>
<td><strong><code>sort -rn</code></strong></td>
<td>按数值逆序排序（<code>-r</code> 降序，<code>-n</code> 按数值比较）。</td>
</tr>
</tbody></table>
<hr>
<h2 id="七、总结与建议"><a href="#七、总结与建议" class="headerlink" title="七、总结与建议"></a>七、总结与建议</h2><h3 id="问题根源："><a href="#问题根源：" class="headerlink" title="问题根源："></a>问题根源：</h3><ul>
<li><strong>日志文件未轮转</strong>：<code>secure</code> 和 <code>messages</code> 未配置自动切割与压缩，导致单文件过大。  </li>
<li><strong>旧文件未清理</strong>：历史日志（如 <code>pacct-*</code>）未定期删除，长期占用空间。</li>
</ul>
<h3 id="优化建议："><a href="#优化建议：" class="headerlink" title="优化建议："></a>优化建议：</h3><ol>
<li><p><strong>配置日志轮转</strong>：  </p>
<ul>
<li>使用 <code>logrotate</code> 工具定期切割、压缩日志文件（如 <code>/var/log/secure</code>）。  </li>
<li>示例配置：  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/var/log/secure &#123;  </span><br><span class="line">    daily  </span><br><span class="line">    rotate 30  </span><br><span class="line">    compress  </span><br><span class="line">    missingok  </span><br><span class="line">    notifempty  </span><br><span class="line">&#125;  </span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>定期清理旧文件</strong>：  </p>
<ul>
<li>通过 cron 任务自动删除超过指定时间的文件（如保留最近3个月）：  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">0 0 * * * find /var/account -name <span class="string">&quot;pacct-*&quot;</span> -mtime +90 -<span class="built_in">exec</span> <span class="built_in">rm</span> -f &#123;&#125; \;  </span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>监控磁盘使用率</strong>：  </p>
<ul>
<li>部署监控工具（如 Prometheus + Grafana）实时报警磁盘使用率阈值（如 &gt;80%）。</li>
</ul>
</li>
<li><p><strong>扩容存储（可选）</strong>：  </p>
<ul>
<li>若业务需求增长，可扩展 <code>/var</code> 所在逻辑卷（LVM）的容量。</li>
</ul>
</li>
</ol>
<h3 id="风险提示："><a href="#风险提示：" class="headerlink" title="风险提示："></a>风险提示：</h3><ul>
<li>直接删除日志文件前，需确认文件是否被进程占用（如 <code>lsof | grep secure</code>）。  </li>
<li><code>pacct</code> 文件为进程记账记录，删除后可能导致审计信息丢失，建议备份重要数据。</li>
</ul>
]]></content>
      <categories>
        <category>Linux文件系统</category>
        <category>var</category>
      </categories>
      <tags>
        <tag>Linux文件系统</tag>
        <tag>var</tag>
      </tags>
  </entry>
</search>
